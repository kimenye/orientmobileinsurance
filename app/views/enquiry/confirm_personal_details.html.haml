#confirm-personal-details{"data-role" => "page", "data-theme" => "b", "data-add-back-btn" => "true"}
  %header{"data-role" => "header"}
    %h1
      Your Device Details
  %section{"data-role" => "content", "role" => "main"}
    %ul{"data-role" => "listview", "data-inset" => "true", "data-theme" => "c", "data-divider-theme" => "b"}
      %li{"data-role" => "list-divider"}
        A quote with the following details has been created
      %li
        %img{:src => "/assets/phone.png", :class =>"ui-li-icon ui-corner-none"}
        %b
          Make & Model:
        %span.model
          = session[:device].marketing_name
      %li
        %img{:src => "/assets/calendar.png", :class =>"ui-li-icon ui-corner-none"}
        %b
          Year of Purchase:
        %span.model
          = @enquiry.year_of_purchase
      %li
        %b
          Insurance value:
        %span.model
          = session[:quote_details]["insurance_value"]
      %li
        %b
          Amount Due:
        %span.model
          - if(session[:user_details]["customer_payment_option"] == 'Annual')
            = session[:quote_details]["annual_premium"]
          - elsif(session[:user_details]["customer_payment_option"] == 'Monthly')
            = session[:quote_details]["quarterly_premium"]
      %li
        %b
          Name:
        %span.model
          = session[:user_details]["customer_name"]
      %li
        %b
          ID or Passport No.:
        %span.model
          = session[:user_details]["customer_id"]
      -#%li
        %b
          Mobile No.:
        %span.model
          = session[:user_details]["customer_phone_number"]
      -#%li
        %b
          Email:
        %span.model
          = session[:user_details]["customer_email"]
      - if !session[:quote_details]["sales_agent"].nil?
        %li
          %b
            Sales Agent:
          %span.model
            = session[:quote_details]["sales_agent"]
      %li
        %span.model
          Your Account Number is:
          = session[:user_details]["account_name"]
          and is valid till
          = "#{session[:quote].expiry_date.strftime("%d-%m-%Y").to_s(:full)}."
          Please click Accept for payment information
      -#%li
      -#  %b
      -#    Amount Due:
      -#  %span.model
      -#    - if(@enquiry.customer_payment_option == 'Annualy')
      -#      = session[:quote_details]["annual_premium"]
      -#    - elsif(@enquiry.customer_payment_option == 'Monthly')
      -#%li
      -#  %b
      -#    Name:
      -#  %span.model
      -#    = @enquiry.customer_name
      -#%li
      -#  %b
      -#    ID or Passport No.:
      -#  %span.model
      -#    = @enquiry.customer_id
      -#%li
      -#  %b
      -#    Sales Agent:
      -#  %span.model
      -#    = @enquiry.sales_agent_code
    %a{"href" => "#", "data-role" => "button", "data-inline" => "true", "data-mini" => "true"} Cancel
    = link_to('Accept', wizard_path(:complete_enquiry), "data-role" => "button", "data-inline" => "true", "data-mini" => "true")

-#:javascript
-#
-#  $("#send-sms").click(function() {
-#      alert("Handler for .click() called.");
-#  });
-#
-#  $(document).ready(function() {
-#
-#     alert("yo");
-#     alert("#{session[:user_details]['customer_phone_number']}");
-#      //alert();
-#      //$.post("enquiries/send_sms", { phone_number: "", message: "" } );
-#
-#  });
